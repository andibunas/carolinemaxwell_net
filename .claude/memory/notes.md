# Session notes

- 2026-09-18: Added a root `README.md` with 3-bullet summaries for each subproject: `dna_office` (React/Vite manifest-driven DNA site, query-string routing), `caroline-maxwell-website` (React Router artist portfolio, manifest-driven layouts, has `build:static` prerender step), `all_dna` (legacy classic ASP.NET/frameset site, source material `dna_office` is rebuilding from).
